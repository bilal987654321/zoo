<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= habitat ? 'Edit Habitat' : 'Add New Habitat' %> - Veterinary Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/navbar', { pageTitle: 'Page Title'}) %>
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header">
                        <h2 class="mb-0"><%= habitat ? 'Edit Habitat: ' + habitat.nom : 'Add New Habitat' %></h2>
                    </div>
                    <div class="card-body">
                        <form action="<%= habitat ? '/api/habitats/' + habitat.habitat_id : '/api/habitats' %>" method="POST" class="needs-validation" novalidate>
                            <div class="mb-3">
                                <label for="nom" class="form-label required">Name</label>
                                <input type="text" class="form-control" id="nom" name="nom" value="<%= habitat ? habitat.nom : '' %>" required>
                                <div class="invalid-feedback">
                                    Please provide a habitat name.
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="description" class="form-label required">Description</label>
                                <input type="text" class="form-control" id="description" name="description" value="<%= habitat ? habitat.description : '' %>" required>
                                <div class="invalid-feedback">
                                    Please provide a description.
                                </div>
                            </div>

                            <div class="mb-3">
                                <label for="commentaire_habitat" class="form-label required">Comment</label>
                                <input type="text" class="form-control" id="commentaire_habitat" name="commentaire_habitat" value="<%= habitat ? habitat.commentaire_habitat : '' %>" required>
                                <div class="invalid-feedback">
                                    Please provide a comment.
                                </div>
                            </div>

                            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                                <a href="/habitats" class="btn btn-secondary me-md-2">Cancel</a>
                                <button type="submit" class="btn btn-primary"><%= habitat ? 'Update Habitat' : 'Add Habitat' %></button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        (function () {
            'use strict';
            var forms = document.querySelectorAll('.needs-validation');
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
        })();
    </script>
</body>
</html>